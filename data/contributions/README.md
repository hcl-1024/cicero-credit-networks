# Contribution Proposals

Place proposed additions and corrections in `proposals/` as JSON files. Copy a template from `examples/`, replace the example UUID, and run `make validate-proposals` and `make preview`.

Proposal files are overlays for review. They never become calculation inputs merely because they exist in a fork or pull request.
